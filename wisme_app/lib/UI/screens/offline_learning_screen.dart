import 'package:flutter/material.dart';

class OfflineLearningScreen extends StatelessWidget {
  const OfflineLearningScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('OfflineLearningScreen')),
      body: const Center(
        child: Text('OfflineLearningScreen - Coming Soon'),
      ),
    );
  }
}
